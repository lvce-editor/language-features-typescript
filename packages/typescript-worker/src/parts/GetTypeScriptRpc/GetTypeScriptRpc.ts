import * as Rpc from '../Rpc/Rpc.ts'

export const getRpc = (path) => {
  // TODO
  // when running in node create websocket rpc
  // when running in electron, create messageport rpc
  throw new Error('not implemented')
}
